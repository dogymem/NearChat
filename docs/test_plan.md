# План тестирования (Test Plan) для проекта NearChat

## Информация о документе
- **Проект:** NearChat (iOS P2P Messenger)
- **Версия:** 1.0
- **Статус:** Утвержден

## Содержание
1. [Введение](#1-введение)
2. [Цели тестирования](#2-цели-тестирования)
3. [Область тестирования (Scope)](#3-область-тестирования-scope)
4. [Стратегия тестирования](#4-стратегия-тестирования)
5. [Виды тестирования](#5-виды-тестирования)
6. [Тестовое окружение](#6-тестовое-окружение)
7. [Тестовые данные](#7-тестовые-данные)
8. [Расписание тестирования](#8-расписание-тестирования)
9. [Артефакты тестирования](#9-артефакты-тестирования)
10. [Оценка рисков](#10-оценка-рисков)
11. [Инструменты тестирования](#11-инструменты-тестирования)

---

## 1. Введение

### 1.1 Назначение
Данный документ описывает стратегию тестирования мобильного приложения NearChat, предназначенного для обмена текстовыми сообщениями в локальной сети по протоколу TCP. План гарантирует проверку функциональности режимов «Сервер» и «Клиент», стабильность сетевого соединения и корректность работы пользовательского интерфейса на SwiftUI.

### 1.2 Охват
План тестирования охватывает все компоненты приложения:
- Сетевой слой (`TCPManager`, `NWListener`, `NWConnection`).
- Управление состоянием приложения (`AppState`).
- Пользовательский интерфейс (`ChatView`, `SettingsView`).
- Локальное хранение данных (`UserDefaults` через `@AppStorage`).
- Взаимодействие устройств в режимах Host-to-Client и Client-to-Client (через Host).

### 1.3 Ссылки
- Техническое задание (SRS) NearChat.
- Диаграммы вариантов использования (Use Cases).
- Диаграммы последовательности (Sequence Diagrams).
- Архитектурная документация проекта.

## 2. Цели тестирования

### 2.1 Основные цели
- Подтвердить корректную работу сетевого протокола TCP (установка соединения, передача данных, разрыв).
- Убедиться в стабильности работы режима «Сервер» при подключении нескольких клиентов.
- Проверить механизм ретрансляции (Broadcast) сообщений всем участникам чата.
- Валидировать работу UI/UX: блокировка экрана при разрывах, отображение статусов.
- Проверить корректность кодирования/декодирования сообщений (Base64) и работу с кириллицей.

### 2.2 Критерии успеха
- Успешное подключение Клиента к Серверу по IP и порту.
- Сообщения доставляются всем участникам чата без искажений и задержек (> 1 сек).
- При разрыве соединения UI корректно блокируется и отображает причину.
- Приложение не крашится при сетевых ошибках (таймауты, занятый порт).

## 3. Область тестирования (Scope)

### 3.1 Входит в область тестирования (In Scope)
- **Управление профилем:** Сохранение, редактирование и загрузка никнейма.
- **Режим Сервера (Host):** Запуск слушателя, определение IP, управление списком подключений.
- **Режим Клиента:** Подключение по IP, переподключение, обработка ошибок соединения.
- **Чат:** Отправка/получение сообщений, автоскролл, отображение истории.
- **Жизненный цикл:** Сворачивание/разворачивание приложения, смена режимов.
- **Сетевая безопасность (базовая):** Работа в локальной сети, разрешение Local Network Privacy.

### 3.2 Не входит в область тестирования (Out of Scope)
- Работа через интернет (WAN) или NAT Traversal.
- Передача файлов, изображений и аудио.
- Push-уведомления (отсутствие APNS).
- Шифрование трафика (используется открытый текст/Base64).
- Поддержка Android или десктопных ОС (только iOS).

## 4. Стратегия тестирования

### 4.1 Подход
- **Модульное тестирование (Unit Testing):** Проверка логики формирования сообщений (`createFullMessage`) и работы `AppState`.
- **Интеграционное тестирование:** Проверка взаимодействия между `ChatView` и `TCPManager`.
- **Системное тестирование (Manual):** Тестирование на реальных устройствах iPhone в одной Wi-Fi сети.
- **Стресс-тестирование:** Проверка поведения при частом переподключении.

### 4.2 Уровни тестирования
1. **Unit Testing:** Тестирование методов класса `TCPManager` (изолированно от сети).
2. **UI Testing:** Автоматизированные/ручные тесты переходов между экранами и ввода текста.
3. **Connectivity Testing:** Проверка физического соединения между устройствами в реальных условиях.

## 5. Виды тестирования

### 5.1 Функциональное тестирование

#### 5.1.1 Управление пользователем
- **Настройка профиля:**
  - Ввод валидного никнейма.
  - Попытка сохранения пустого никнейма (валидация кнопки).
  - Сохранение никнейма между перезапусками приложения.

#### 5.1.2 Режим Сервера
- **Запуск:**
  - Корректное определение IP-адреса Wi-Fi интерфейса (`en0`).
  - Успешный старт `NWListener` на порту 12345.
  - Обработка ошибки, если порт занят другим процессом.
- **Управление клиентами:**
  - Принятие входящего подключения.
  - Отключение клиента (корректное удаление из массива `connections`).

#### 5.1.3 Режим Клиента
- **Подключение:**
  - Успешное подключение к существующему серверу.
  - Ошибка подключения к несуществующему IP (таймаут).
  - Ошибка подключения при неверном порте.
- **Состояние:**
  - Отображение статусов: "В ожидании", "Подключение...", "Подключено", "Ошибка".

#### 5.1.4 Обмен сообщениями
- **Отправка:**
  - Отправка текста с Сервера (функция Broadcast).
  - Отправка текста от Клиента (отправка на Сервер -> Broadcast остальным).
- **Отображение:**
  - Различение своих и чужих сообщений (цвета пузырей, выравнивание).
  - Отображение системных сообщений ("Клиент подключился", "Отключено").
  - Корректный парсинг Timestamp и Nickname из строки формата `Time:Base64:Base64`.

### 5.2 Нефункциональное тестирование

#### 5.2.1 Тестирование производительности
- **Задержка (Latency):** Передача сообщения < 200 мс в локальной сети.
- **Нагрузка:** Отправка частых сообщений (быстрый ввод).
- **Количество клиентов:** Стабильная работа при подключении 3-5 устройств.

#### 5.2.2 UI/UX и Юзабилити
- **Отзывчивость:** Интерфейс не блокируется при сетевых операциях (работа в `DispatchQueue.global`).
- **Обратная связь:** Оверлей блокировки экрана при потере связи с понятным текстом ошибки.
- **Адаптивность:** Корректное отображение на iPhone 13 Pro.
- **Клавиатура:** Корректное скрытие клавиатуры и скролл чата к последнему сообщению.

#### 5.2.3 Тестирование надежности (Recovery)
- **Разрыв Wi-Fi:** Поведение приложения при отключении Wi-Fi на устройстве.
- **Сворачивание:** Работа сокетов при переходе приложения в фон.
- **Перезапуск сервера:** Автоматическое отключение клиентов при остановке сервера.

## 6. Тестовое окружение

### 6.1 Оборудование (Hardware)
- **Устройства:** Минимум 2 физических устройства Apple (iPhone/iPad).
- **Симуляторы:** Xcode Simulator (ограниченная поддержка P2P).
- **Сеть:** Локальный Wi-Fi роутер со стабильным сигналом.

### 6.2 Программное обеспечение (Software)
- **OS:** iOS 15.0 и выше.
- **Инструменты:** Xcode 14+, Network Link Conditioner (для имитации плохой сети).

### 6.3 Подготовка окружения
- Все устройства подключены к одной Wi-Fi сети.
- На устройствах выдано разрешение "Local Network Privacy" для приложения NearChat.

## 7. Тестовые данные

### 7.1 Пользователи
- Никнеймы на латинице ("User1").
- Никнеймы на кириллице ("Пользователь").
- Никнеймы со спецсимволами и эмодзи ("User🔥").
- Длинные никнеймы (проверка верстки пузырей).

### 7.2 Сообщения
- Короткие сообщения ("Привет").
- Длинные сообщения (несколько строк).
- Сообщения с символами разделителями (двоеточие `:`, перенос строки `\n`) — проверка Base64.

### 7.3 Сетевые параметры
- IP-адреса: Корректные (192.168.x.x), Локальные (127.0.0.1), Некорректные.
- Порты: Стандартный (12345), Системные/Занятые.

## 8. Расписание тестирования

### 8.1 Фазы
1. **Unit Testing:** Проверка методов модели данных и парсинга сообщений.
2. **Integration Testing (Simulators):** Проверка UI и навигации.
3. **System Testing (Physical Devices):** Полноценный тест чата между устройствами.
4. **Bug Fixing:** Исправление найденных проблем соединения и верстки.
5. **Acceptance Testing:** Финальная проверка перед демонстрацией.

## 9. Артефакты тестирования

### 9.1 Документация
- План тестирования (текущий документ).
- Чек-лист функциональности.
- Отчеты о дефектах (Bug Reports).
- Отчет о завершении тестирования.

### 9.2 Результаты
- Логи консоли Xcode при сбоях подключения.
- Скриншоты UI при различных состояниях и ошибках.

## 10. Оценка рисков

### 10.1 Идентифицированные риски

#### Высокий риск
- **iOS блокирует доступ к локальной сети:** Пользователь не выдал разрешение Privacy.
  - *Стратегия:* Добавить проверку статуса разрешения и инструкцию.
- **Разрыв соединения в фоне:** iOS может "убить" сокет при сворачивании.
  - *Стратегия:* Тестирование жизненного цикла, уведомление о разрыве.

#### Средний риск
- **Конфликт IP/Портов:** Порт 12345 занят другим приложением.
  - *Стратегия:* Обработка ошибки `.failed` при запуске `NWListener`.
- **Смена IP роутером (DHCP):** IP сервера изменился во время сессии.
  - *Стратегия:* Отображение актуального IP в настройках в реальном времени.

#### Низкий риск
- **Проблемы кодировки:** Разные локали устройств.
  - *Стратегия:* Использование UTF8 и Base64 везде.

## 11. Инструменты тестирования

### 11.1 Разработка и отладка
- **Xcode:** Основная среда разработки и тестирования.
- **XCTest:** Фреймворк для Unit и UI тестов.
- **LLDB:** Отладка крашей.

### 11.2 Сетевой анализ
- **Network Link Conditioner:** Имитация потери пакетов, высокой задержки, DNS задержек.
- **Wireshark:** (Опционально) Анализ TCP пакетов при проблемах с протоколом.

### 11.3 Управление тестированием
- **TestFlight:** Дистрибуция билдов для внешних тестеров (если применимо).
- **GitHub Issues:** Трекинг задач и багов.

---

**Контроль документа**
- **Версия:** 1.0
- **Дата:** 20.11.2025
- **Автор:** QA Team